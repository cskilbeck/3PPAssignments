@model Who.Models.RegionsSet

@{
    ViewBag.Title = "Regions";
}
                <table>
                    <tr>
                        <th>Regions</th>
                    </tr>
                    @foreach (var item in Model.Regions) {
                    <tr>
                        <td>
                            @Html.ActionLink(@item.Value.Name, "Index", "Home", new { id = item.Value.GUID, name=item.Value.Name }, null )
                        </td>
                    </tr>
                    }
                </table>
